export interface Item {
  name: string;
  price: number;
  quantity: number;
}

export function merge(left: Item[], right: Item[]): Item[] {
  return [];
}

export function mergeSort(arr: Item[]): Item[] {
  return [];
}
